<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".MotoVoiceApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:enableOnBackInvokedCallback="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MotoVoice">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            ACTION_ASSIST activity — this is what makes the app appear in
            Settings > Default apps > Digital assistant app on all Android devices.
            Triggered by long-pressing Home, BVRA Bluetooth button, or RoleManager.ROLE_ASSISTANT.
            It immediately starts VoiceCommandService and finishes with no visible UI.
        -->
        <activity
            android:name=".VoiceAssistActivity"
            android:theme="@android:style/Theme.NoDisplay"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.ASSIST" />
                <action android:name="android.intent.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:label="@string/title_settings"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".debug.DebugLogActivity"
            android:exported="false"
            android:label="@string/title_debug_log"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:label="@string/title_history"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".FavoritesActivity"
            android:exported="false"
            android:label="@string/title_favorites"
            android:parentActivityName=".MainActivity" />

        <!-- RidingModeActivity removed in v1.3.16 — rider keeps phone in pocket -->

        <activity
            android:name=".OnboardingActivity"
            android:exported="false"
            android:label="@string/title_onboarding"
            android:parentActivityName=".MainActivity" />

        <activity
            android:name=".SystemStatusActivity"
            android:exported="false"
            android:label="@string/title_system_status"
            android:parentActivityName=".SettingsActivity" />

        <!-- Quick Settings tile — start listening without opening the app. -->
        <service
            android:name=".tile.MotoVoiceTileService"
            android:exported="true"
            android:icon="@android:drawable/ic_btn_speak_now"
            android:label="Moto Voice"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Foreground service that runs the voice pipeline (microphone) -->
        <service
            android:name=".service.VoiceCommandService"
            android:foregroundServiceType="microphone"
            android:exported="false" />

        <!-- FM radio streaming service (mediaPlayback). Exported so system media controls
             and Android Auto can connect via MediaSessionService. -->
        <service
            android:name=".media.FmPlayerService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!--
            VoiceInteractionService — additional registration for devices that support
            full VIS (e.g. stock Android). Not required for the assistant list.
        -->
        <service
            android:name=".service.MotoVoiceInteractionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/interaction" />
        </service>

        <service
            android:name=".service.MotoVoiceSessionService"
            android:permission="android.permission.BIND_VOICE_INTERACTION_SERVICE"
            android:exported="true" />

        <!--
            v1.3.11 §1.1 — presence-only NotificationListenerService. Required for
            android.media.session.MediaSessionManager.getActiveSessions(), which is
            what lets us READ playbackState from YouTube/Spotify/etc and control
            them via MediaController.transportControls (play/pause/seek). We don't
            consume notification callbacks at all — the service body is empty. The
            user grants "Notification access" in Settings; every code path falls
            back to media-key dispatch when the permission is denied so nothing
            breaks.
        -->
        <service
            android:name=".bt.MotoMediaNotificationListener"
            android:label="Moto Voice Media Access"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>

        <!-- Preflight-on-boot notification so the rider knows before the first BVRA press -->
        <receiver
            android:name=".bt.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>

</manifest>
